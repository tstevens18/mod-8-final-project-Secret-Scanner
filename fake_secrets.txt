# These look like secrets but do not match the regex patterns.

# Fake AWS (missing AKIA + 16 exact chars)
aws_access_key_id = "akia-1234-fake-key"

# Fake AWS Secret (not 40 chars, no proper pattern)
aws_secret_access_key = "notarealsecretkey123"

# Fake API keys (wrong names or too short)
user_api = "abc123"
apiToken = "fake-token-999"

# Fake passwords (missing quotes or wrong syntax for the regex)
password: mypassword123
Password = mypassword123
passwd -> "not detected"

# Fake JWT (break the structure so the regex doesn't match)
jwt_token = "eyFakeHeader.eyFakePayload.NotARealSignature"

# Fake private key block (uppercase/lowercase mismatch)
-----Begin Private Key-----
FAKEPRIVATEKEYDATA1234567890
-----End Private Key-----

# Fake GitHub token (wrong format)
ghp_fake_token_12345

# Fake Slack token (missing prefix xoxb-)
slack_token="fake123456789"

# Random strings that look sensitive but aren't
secretKeyExampleHereButNotReal
my_private_key_is_fake
SUPERSECRET=123456789

# Short dummy keys
api_key = "short"
apikey = '12345'
api-key = testing

# Base64-ish strings that won't match JWT or AWS patterns
YXNkZmFrZXN0cmluZw==
dGhpc2lzbm90YWp3dA==
